<h1>New Book</h1>
<%= form_for @book, url: book_path, method: :put do |f| %>
	<h4>title</h4>
	<%= f.text_field :title %>
	<h4>body</h4>
	<%= f.text_area :body %>
	<%= f.submit 'Update Book' %>
<% end %>
<p><%= link_to 'Show', book_path(@book) %>|<%= link_to "戻る", :back %></p>